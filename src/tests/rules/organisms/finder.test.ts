describe("symetric organisms", () => {
  test.skip("should find a single block", () => {
    // const matrix: Matrix = [
    //   [DEAD, DEAD, DEAD, DEAD],
    //   [DEAD, DEAD, DEAD, DEAD],
    //   [DEAD, DEAD, ALIVE, ALIVE],
    //   [DEAD, DEAD, ALIVE, ALIVE],
    // ];
    // const finder = new OrganismFinder(matrix);
    // expect(finder.organisms.length).toBe(1);
    // expect(finder.organisms[0].name).toBe("block");
  });
});
